spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/

  datasource:
    url: jdbc:mysql://localhost:3306/jay_ecommerce
    username: root
    password: Jay@271203
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8081

# application mesages will goes here
  auth-service:
  messages:
    userReg: User Created
    userRegErr: User Creation Failed
    login: Login Success
    loginErr: Invalid Credentials

